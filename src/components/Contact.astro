<aside id="contact">
    <h2>Feeling curious or you're bored ? Let's have a chat!</h2>

    <form>
        <label>
            Name
            <input type="text" name="name" class="input" />
        </label>

        <label>
            Email
            <input type="email" name="email" class="input" />
        </label>

        <label>
            Message
            <textarea name="message" class="input" rows="4"></textarea>
        </label>

        <button type="submit">Send</button>
    </form>
</aside>

<style>
    aside {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 3rem;
        border-top: 1px solid var(--gray-800);
        border-bottom: 1px solid var(--gray-800);
        padding: 5rem 1.5rem;
        background-color: var(--gray-999_40);
        box-shadow: var(--shadow-sm);
    }

    h2 {
        font-size: var(--text-xl);
        text-align: center;
        max-width: 15ch;
        color: #FFD68A;
    }

    form {
        display: flex;
        flex-direction: column;
        gap: 1rem;
        max-width: 60ch;
        width: min(100%, 60ch);
    }

    label {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
    }

    input,
    textarea {
        padding: 0.5rem;
        border: 1px solid var(--gray-800);
        border-radius: 0.25rem;
        background-color: var(--gray-999_40);
        color: var(--gray-100);
    }

    input:focus,
    textarea:focus {
        outline: none;
        border-color: var(--gray-100);
    }

    button {
        padding: 0.5rem 1rem;
        border: 1px solid var(--gray-800);
        border-radius: 0.25rem;
        background-color: var(--gray-999_40);
        color: var(--gray-100);
        font-weight: 600;
        cursor: pointer;
    }

    button:hover {
        background-color: var(--gray-999_60);
    }

    button:focus {
        outline: none;
        border-color: var(--gray-100);
    }

    @media (min-width: 50em) {
        aside {
            padding: 7.5rem;
            flex-direction: row;
            flex-wrap: wrap;
            justify-content: space-between;
        }

        h2 {
            font-size: var(--text-3xl);
            text-align: left;
        }
    }
</style>
